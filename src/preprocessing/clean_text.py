"""
축제 비정형 텍스트 정제 모듈

역할
- API 응답의 빈 값을 정규화한다.
- HTML 태그를 제거한다.
- 과도한 공백과 줄바꿈을 정리한다.

중요
이번 프로젝트에서는 Relation마다 evidence를 남기고
evidence가 실제 원문에 존재하는지 검증해야 한다.

따라서 원문의 의미나 문장 표현을 변경하는 전처리는 하지 않는다.

하지 말 것
- 문장 요약
- 맞춤법 전체 교정
- 동의어 치환
- 문장 재작성
- LLM을 이용한 문장 정제

해도 되는 것
- HTML 태그 제거
- 빈 문자열 처리
- 과도한 공백 정리
- 불필요한 제어문자 제거
"""


# TODO: 빈 값을 하나의 결측 표현으로 통일하는 함수를 작성한다.
#
# 함수명 예시
# normalize_missing_value(value)
#
# 처리 대상 예시
# ""
# " "
# "\n"
# None
#
# 해야 할 일
# 1. 입력값이 None인지 확인한다.
# 2. 문자열이면 앞뒤 공백을 제거한 결과가 비어 있는지 확인한다.
# 3. 실제 내용이 없다면 결측값으로 처리한다.
# 4. 값이 있다면 원래 내용을 유지한다.


# TODO: HTML 태그를 제거하는 함수를 작성한다.
#
# 함수명 예시
# remove_html(text)
#
# 처리 대상 예시
# <br>
# <br />
# <strong>
# <a href="...">
#
# 해야 할 일
# 1. 입력 텍스트가 비어 있는지 확인한다.
# 2. HTML을 파싱한다.
# 3. 태그를 제거한다.
# 4. 사람이 읽을 수 있는 실제 텍스트만 반환한다.
#
# 주의
# - 태그만 제거하고 문장 내용은 재작성하지 않는다.


# TODO: 공백과 줄바꿈을 정리하는 함수를 작성한다.
#
# 함수명 예시
# normalize_whitespace(text)
#
# 해야 할 일
# 1. 반복되는 공백을 정리한다.
# 2. 필요 이상으로 반복되는 줄바꿈을 정리한다.
# 3. 문자열 앞뒤 공백을 제거한다.
# 4. 정리된 문자열을 반환한다.
#
# 매우 중요
# evidence 원문 대조가 가능하도록 문장 의미와 표현은 변경하지 않는다.


# TODO: 텍스트 전처리 전체 과정을 한 번에 실행하는 대표 함수를 작성한다.
#
# 함수명 예시
# clean_text(text)
#
# 처리 순서
# 1. 결측값 처리
# 2. HTML 제거
# 3. 공백 정규화
# 4. 최종 텍스트 반환
#
# 다른 모듈에서는 가능하면 개별 정제 함수를 직접 호출하기보다
# 이 대표 함수를 사용하도록 한다.