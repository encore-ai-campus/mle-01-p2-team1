"""전처리 5단계: 검증과 결과 출력 TODO."""

# TODO 1. validate_document(document) -> list[str]를 구현한다. 모든 오류를 반환한다.
# TODO 2. make_reject_log(document, errors) -> dict를 구현한다. 본문 전체는 복사하지 않는다.
# TODO 3. save_jsonl(path, rows)와 save_json(path, value)를 UTF-8로 구현한다.
# TODO 4. build_preprocessing_report(processed, rejects, source_count)를 구현한다.
# TODO 5. config의 세 출력 경로를 사용한다.
# TODO 6. MIN_TEXT_LENGTH, YYYYMMDD, 경도 -180~180, 위도 -90~90 기준을 정의한다.
# TODO 7. 재실행 시 JSONL을 append하지 않고 overwrite한다.
# TODO 8. 저장 후 JSON 파싱, source 합계, doc_id 중복, report 통계를 재검증한다.
